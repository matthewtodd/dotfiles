# for the format of this file, see curl(1)'s --config option

# I'm somewhat surprised --compressed isn't on by default
--compressed

# Attempt to resume broken downloads
--continue-at -

# Always follow redirects
--location
