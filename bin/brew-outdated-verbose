#!/usr/bin/env ruby

# My current version of homebrew (0.7) hides the version numbers from `brew
# outdated` if $stdout isn't a tty. But I have a weekly cron job spitting
# outdated package names into my Things inbox, and I'd like to see the version
# numbers there, too. So this script fakes it.
def $stdout.tty?
  true
end

ARGV.replace ['outdated']

load `which brew`.chomp
