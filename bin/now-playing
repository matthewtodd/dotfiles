#!/usr/bin/env ruby

require 'rubygems'
require 'appscript'

if `ps axww`.grep(%r{MacOS/iTunes\b}).any?
  itunes = Appscript.app('iTunes')

  if itunes.player_state.get == :playing
    track = itunes.current_track
    puts "♫ #{track.artist.get}: #{track.name.get} ♫"
  end
end
