#!/bin/bash

set -euo pipefail

# Be good about whitespace even when a test fails:
trap echo ERR

while read -r command; do
  echo "$(tput setaf 6)$command$(tput sgr0)"
  eval $command
  echo
done < <(git config --get-all tcr.command)
