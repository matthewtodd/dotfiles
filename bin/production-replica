#!/bin/sh

# op account list
export OP_ACCOUNT=ZOB2F7NW2ZEG7HAYE5DBCM526E

# op vault list
VAULT=p3hh25rpgtgb6codrqakfh2o3y
# op item list --vault VAULT
ITEM=vy6j52fj24tf6lox2fvsm4rnhu

export PROD_REPLICA_PASSWORD="op://${VAULT}/${ITEM}/password"

# NOTE(mt): This only works when standing in the project directory because I
# was too lazy to figure out triggering the rbenv/mise hooks in this script.
op run --no-masking -- \
  ./bin/rails_console_production_replica
